const express = require("express");
const router = express.Router();

router.get("/", (req, res) => {
  // const session = sessionStorage.getItem("sessionUsername")
  // console.log("session now: " + session)
});

module.exports = router;
